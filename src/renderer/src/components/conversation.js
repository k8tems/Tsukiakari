import React, { Component } from 'react';
import B from '../lib/bem';

const b = B.with('conversation');

export default class Conversation extends Component {
  render() {
    return (<div></div>);
  }
}
